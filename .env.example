# Database
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Auth.js (NextAuth) — Google OAuth
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# OpenAI API — 필수
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# OpenAI 호환 엔드포인트(Azure OpenAI, OpenRouter 등)를 쓸 때만 설정
# OPENAI_BASE_URL=
